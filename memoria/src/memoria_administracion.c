#include "../include/memoria_administracion.h"

//acá se desarrollan funciones paracumplir con las peticiones que llegan de CPU, Kernel y Entrada-Salida

espacio_usuario = malloc();
puntero_espacio_usuario = espacio_usuario;

void crear_proceso(){ //instancia una estructura proceso
    // creo que acá iría la lógica de recepción de un paquete con el mensaje de que se quiere crear un proceso o puede ir afuera de esta función y que la función reciba lo que se nos envió. 


}